FROM node:20-alpine

RUN npm config set strict-ssl false && \
    npm install -g pnpm@10.0.0 && \
    pnpm config set strict-ssl false

WORKDIR /app

CMD ["sh", "-c", "pnpm --filter @plataforma/contracts build && pnpm --filter web exec next dev -H 0.0.0.0 -p 3000"]
